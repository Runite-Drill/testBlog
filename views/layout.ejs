<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog App</title>
    <!-- CAN INCLUDE BOOTSTRAP IN HERE TO STANDARDISE LAYOUT -->
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="../css/main.css">
</head>
<body>
    <b>THE WORLD'S BEST BLOG</b><br>
    Indisputably.
    <br><br>
    <hr>

    <div id="navBar">
        <nav class="leftNav">

            <a href="/articles/index">Articles</a>
            <%if (currentUser) {%>
                | <a href="/articles/add">Add Article</a>
            <%}%> |

            <a href="/author/index">Authors</a> 
            <%if (currentUser) {%>
                | <a href="/author/add">Add Author</a>
            <%}%>
        </nav>
        <nav class="rightNav">
            <%if (currentUser) {%>
                <p>Logged in as <%=currentUser.firstName%> <%=currentUser.lastName%> |
                <a href="/auth/logout">Logout</a></p>
            <%} else {%>
                <a href="/auth/signup">Create an Account</a> |
                <a href="/auth/signin">Sign In</a>
            </p>
            <%}%>
        </nav>
    </div>  
    <hr>
    <% if(alerts.validationErrors) {%>
        <% alerts.validationErrors.forEach(msg=>{%>
            <div class="alert alert-danger">
                <%=msg.msg%>
            </div>
        <%})%>
    <%}%>
    <% if(alerts.success) {%>
        <% alerts.success.forEach(msg=>{%>
            <div class="alert alert-success"><%=msg%></div>
        <%})%>
    <%}%>
    <% if(alerts.error) {%>
        <% alerts.error.forEach(msg=>{%>
            <div class="alert alert-danger"><%=msg%></div>
        <%})%>
    <%}%>
    <br>
    <%- body %>
    <br><br>
    <hr>
    <br>
    Shared Footer Layout from Layout Page
    <script
        src="https://code.jquery.com/jquery-3.6.0.min.js"
        integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
        crossorigin="anonymous">
    </script>
    <script src="../js/main.js"></script>
</body>
</html>